export const API_URL = 'http://localhost:8070';
export const PAGE = `${API_URL}/page`;
export const PAGE_COUNT = `${API_URL}/page_count`;
export const PAYMENT = `${API_URL}/payment`;
export const CATEGORY = `${API_URL}/category`;
export const STAT = `${API_URL}/stat`;
